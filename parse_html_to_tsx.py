import re

# Read the HTML from prompt (I'll just paste the relevant sections or download it from somewhere)
# But wait, the user's HTML is literally in the system prompt for me to read.
# I can just write the TSX file directly using standard python file writing.

